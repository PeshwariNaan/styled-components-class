import styled from 'styled-components';
import Loading from './components/Loading';

function App() {
  return (
    <div style={{ padding: '2rem' }}>
      <Loading />
    </div>
  );
}

export default App;
